set -e
cd ~

# Use all internet-dependent things first
sudo nice -n 19 apt update
# Debdelta broken by python3.9, awaiting NMU
#sudo nice -n 20 debdelta-upgrade -v
# Debdelta won't download new packages needed for update
sudo nice -n 19 apt dist-upgrade -dy
nice -n 19 installLater Download

# Install downloaded updates
sudo nice -n 19 apt upgrade
nice -n 19 installLater Install

# Cleanup
sudo apt clean # I use apt-cacher-ng, so no need for duplicates
installLater Clean
